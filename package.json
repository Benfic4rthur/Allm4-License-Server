{
  "name": "allm4-license-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Servidor de licencas e pagamentos do Allm4",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check src/app.js && node --check src/db.js && node --check src/security.js && node --check src/license-service.js && node --check src/license-routes.js && node --check scripts/migrate.js && node --check api/index.js && node --check server.js && node --check test/security.test.js",
    "test": "node --test",
    "db:migrate": "node scripts/migrate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.13.1"
  }
}
